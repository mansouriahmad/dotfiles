# macOS packages, installed by install.sh via `brew bundle`.
# Linux gets the same tools from apt and upstream releases (see install.sh).
# Toolchains (nvm, rustup, .NET, Miniconda) are not here: install.sh puts them
# under $DATA_ROOT on both platforms.

brew "git"
brew "wget"
brew "cmake"
brew "pkg-config"
brew "ripgrep"
brew "fd"
brew "eza"
brew "btop"
brew "neovim"
brew "zellij"
brew "starship"
brew "lazygit"
brew "openjdk@17"

cask "wezterm"
cask "font-jetbrains-mono-nerd-font"
